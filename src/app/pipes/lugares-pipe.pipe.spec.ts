import { LugaresPipePipe } from './lugares-pipe.pipe';

describe('LugaresPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LugaresPipePipe();
    expect(pipe).toBeTruthy();
  });
});
